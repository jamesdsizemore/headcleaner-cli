feat(v0.7.0): Batch 6 - serve/glob REPL/PDF progress + PST per-message + Attest/Notion/VSCode full impls

Final implementation of all outstanding items from the original backlog:

Implementation (full, not stub):
- src/headcleaner/serve.py: 7 FastAPI routes for bundle browsing
- src/headcleaner/glob_repl.py: Textual UI with live match count
- src/headcleaner/engines/pdf.py: per-page progress callback
- src/headcleaner/engines/pst.py: per-message extraction (one OKF concept per email)
- src/headcleaner/attest.py: RFC 9162 Merkle root + ed25519 signing
- src/headcleaner/notion.py: full Notion export reversal
- vscode-extension/: Concept Explorer + Trust Inspector side panels
- src/headcleaner/cli.py: notion-import + verify subcommands

Test suite: 135 -> 189 (+54 tests)
- 18 attest tests
- 14 notion tests
- 22 vscode extension tests

Version: 0.6.0 -> 0.7.0
Tag: v0.7.0

ENGINE PROGRESS HOOK: Adapter.extract() now accepts progress=Callable[[int, int], None].
All 13 adapters updated to accept the kwarg. Runner pipelines it through to
opts.on_engine_progress(adapter.name, cur, total).

MULTI-CONCEPT CONCEPT: Adapter.extract_messages() returns a list of extracted
dicts; default wraps extract() returning [single]. Overrides signal "one
source -> many concepts" to the runner. PstAdapter uses this for per-message
emission with derived relpaths (archive-0001.md, archive-0002.md, ...).

ATTEST FORMAT:
{
  "tool": "headcleaner",
  "version": "0.7.0",
  "bundle_root": "...",
  "concept_count": N,
  "concepts": {"relpath": "sha256", ...},
  "merkle_root": "<RFC 9162>",
  "public_key": "ed25519:base64" | null,
  "signature": "base64" | null,
  "proof": {"relpath": ["sibling1", "sibling2", ...], ...}
}

CLI DELTAS:
+ headcleaner serve BUNDLE [--host] [--port]
+ headcleaner verify BUNDLE [--public-key] [--attestation]
+ headcleaner notion-import EXPORT.zip OUT

BREAKING: test_batch4_skeletons.py updated to assert real Merkle root +
real Notion import behavior (was asserting stub behaviour).
