# Development records

This directory contains the active phase marker, documentation policy, and exhaustive phase audits. Create a local `.plans/` plan before implementation; it is durable working context but not a release artifact.

Before phase closure, complete `phase-audits/<phase>.json` and run `python scripts/verify_docs.py --phase <phase>`.
